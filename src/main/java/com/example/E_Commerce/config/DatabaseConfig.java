package com.example.E_Commerce.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Profile;

@Configuration
public class DatabaseConfig {
    // Database configuration is handled by application.yml and application-prod.properties
    // This class can be used for custom database configurations if needed
    
    // Production-specific database configurations can be added here
    // For example, connection pool settings, transaction management, etc.
}
